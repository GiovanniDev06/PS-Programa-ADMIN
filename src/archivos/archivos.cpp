#include <iostream>
#include "archivos.h"

// STUB - Persona 2 implementará esto
void archivos_listar(const char* ruta) { std::cout << "[archivos] TODO: listar " << ruta << std::endl; }
void archivos_copiar(const char* origen, const char* destino) { std::cout << "[archivos] TODO: copiar" << std::endl; }
void archivos_mover(const char* origen, const char* destino) { std::cout << "[archivos] TODO: mover" << std::endl; }
void archivos_eliminar(const char* ruta) { std::cout << "[archivos] TODO: eliminar " << ruta << std::endl; }
void archivos_buscar(const char* patron) { std::cout << "[archivos] TODO: buscar " << patron << std::endl; }
void archivos_estadisticas(const char* ruta) { std::cout << "[archivos] TODO: estadisticas " << ruta << std::endl; }
